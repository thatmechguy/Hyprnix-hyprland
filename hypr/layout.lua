-- local colors = require("colors")

hl.config {
    general = {
        gaps_in = 3,
        gaps_out = 6,

        border_size = 2,

        -- col = {
        --     active_border   = colors.primary,
        --     inactive_border = colors.surface,
        -- },
        allow_tearing = true,
    }
}